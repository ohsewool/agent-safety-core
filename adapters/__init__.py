"""Adapters that put existing tools under the core rules.

A regular package, as it always was. `core` was the one shipped without an
`__init__.py`, and `core/__init__.py` records why that mattered.

`adapters` is a name many projects use, and this one is installed at top level -
see `docs/PACKAGING.md` for what that collides with.
"""
